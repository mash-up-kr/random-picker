import styled from '@emotion/styled';
import { removedItemWidth } from '@/components/home/RemovedItem/RemovedItem.styled';

export const RemovedContainer = styled.div`
  position: relative;
  width: ${removedItemWidth + 58}px;
  margin: 20px 20px 0 0;
`;
